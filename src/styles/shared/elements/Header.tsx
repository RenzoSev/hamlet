import { Appbar } from 'react-native-paper';
import styled from 'styled-components/native';

export const SharedHeader = styled(Appbar.Header)``;

export const SharedHeaderText = styled(Appbar.Content)``;

export const SharedHeaderAction = styled(Appbar.Action)``;

export const SharedHeaderBackAction = styled(Appbar.BackAction)``;
